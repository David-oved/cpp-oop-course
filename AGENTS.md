# AGENTS.md — מדריך לסוכני AI ולמפתחים חדשים בפרויקט

> **קרא את הקובץ הזה במלואו לפני שאתה כותב שורת קוד אחת בפרויקט.**
>
> הפרויקט מפותח על ידי כמה אנשים וכמה סוכני AI במקביל, דרך Git. המסמך הזה הוא
> "מקור האמת" המשותף: מה קיים, מה עובד, מה עדיין לא נכתב, איך העיצוב בנוי, במה
> גרסת המובייל שונה מגרסת הדפדפן, ומה כללי העבודה בגיט. אם משהו במסמך סותר את
> הקוד — **הקוד צודק, והמסמך צריך עדכון באותו commit**.
>
> **★ ואם אתה קורא רק חלק אחד — קרא את §1 ואת §6.** הפרויקט אינו אפליקציה
> לקורס אחד: הוא **מנוע** שבעתיד יקבל מצגות שהמשתמש מעלה ויבנה מהן שיעורים
> אינטראקטיביים בעצמו, באמצעות AI. זה משנה את ההגדרה של "פתרון נכון" כמעט
> בכל משימה.

**מסמכים משלימים:**

| קובץ | מה יש בו |
|---|---|
| `README.md` | תיעוד למשתמש/סטודנט: הרצה, מפתחות API, קיצורי מקלדת |
| `MOBILE_UX_SPEC.md` | המפרט המחייב לחוויית המובייל (חלקו כבר מיושם — ראה §11) |
| `AGENTS.md` (המסמך הזה) | תיעוד הנדסי מלא לסוכנים ולמפתחים |

---

## תוכן העניינים

1. [סקירה כללית והחזון](#1-סקירה-כללית-והחזון)
2. [סטאק טכנולוגי ומפת קבצים](#2-סטאק-טכנולוגי-ומפת-קבצים)
3. [הרצה, בנייה ופריסה](#3-הרצה-בנייה-ופריסה)
4. [ארכיטקטורה וזרימת המידע](#4-ארכיטקטורה-וזרימת-המידע)
5. [מערכת התוכן — הסכימה](#5-מערכת-התוכן--הסכימה)
6. [★ ייבוא מצגות משתמש — החזון והשלכותיו על הקוד](#6--ייבוא-מצגות-משתמש--החזון-והשלכותיו-על-הקוד)
7. [מנוע ההרצה: Wandbox + עטיפה חכמה](#7-מנוע-ההרצה-wandbox--עטיפה-חכמה)
8. [שכבת ה-AI (שלושה ספקים)](#8-שכבת-ה-ai-שלושה-ספקים)
9. [התחברות, אחסון ו-PWA](#9-התחברות-אחסון-ו-pwa)
10. [מערכת העיצוב](#10-מערכת-העיצוב)
11. [מובייל מול דפדפן — ההבדלים המלאים](#11-מובייל-מול-דפדפן--ההבדלים-המלאים)
12. [מה עובד היום](#12-מה-עובד-היום)
13. [מה חסר / לא גמור](#13-מה-חסר--לא-גמור)
14. [מלכודות ידועות — אל תחזור עליהן](#14-מלכודות-ידועות--אל-תחזור-עליהן)
15. [כללי עבודה בגיט לצוות מרובה סוכנים](#15-כללי-עבודה-בגיט-לצוות-מרובה-סוכנים)
16. [צ'קליסט לפני סיום משימה](#16-צקליסט-לפני-סיום-משימה)

---

## 1. סקירה כללית והחזון

**מה זה היום:** אפליקציית לימוד אינטראקטיבית (SPA) לקורס "תכנות מתקדם ב-C++"
של אפרת עמר, המרכז האקדמי לב. הקורס כולו בעברית, RTL, ומבוסס על 13 מצגות
מקוריות.

**קהל היעד:** סטודנטים בשנה א' שסיימו קורס C ולא מכירים OOP.

### ★ החזון — קרא את זה לפני שאתה מתכנן משימה

> **13 המצגות של הקורס הן שלב ראשון, לא המוצר הסופי.**
>
> המטרה בעתיד: **המשתמש יעלה מצגות משלו — כל מצגת, כל קורס — וה-AI יבנה מהן
> שיעור אינטראקטיבי מלא, חכם, באיכות זהה לחלוטין לשיעורים שכתובים היום בקוד.**
> אותם בלוקים, אותם חידונים, אותן דיאגרמות זיכרון, אותה הרצת קוד אמיתית.
>
> כלומר: **האפליקציה הזו היא מנוע כללי, שהקורס של אפרת עמר הוא המופע הראשון
> שלו** — לא אפליקציה ייעודית לקורס אחד שאולי יורחב יום אחד.

**ההשלכה המעשית עליך, הסוכן:** כל החלטה שאתה מקבל צריכה לעבוד גם על תוכן
ש**נוצר בזמן ריצה על ידי AI**, לא רק על תוכן שכתוב ידנית ומהודר לתוך ה-bundle.
כל קוד שמניח "התוכן הוא שלנו", "התוכן ידוע בזמן קומפילציה", "התוכן תמיד תקין"
או "יש בדיוק 13 שיעורים" — **מייצר חוב טכני שיחסום את הפיצ'ר המרכזי של
הפרויקט**. הפירוט המלא, כולל מה כבר מוכן לזה ומה עדיין לא, נמצא ב-**§6 — קרא
אותו במלואו**.

**עקרונות מוצר שמכתיבים החלטות טכניות:**

1. **הקוד באמת רץ.** אין סימולציה. כל דוגמה נשלחת ל-g++ 13.2 אמיתי דרך Wandbox
   ומחזירה פלט/שגיאות אמיתיות — כולל שגיאות לינקר, שזו הנקודה הלימודית.
2. **קומפילציה נפרדת אמיתית.** כל `.cpp` מתקמפל לחוד ואז מקושר, כמו ב-Visual
   Studio. זה מה שמאפשר להדגים `undefined reference` ו-`multiple definition`.
3. **אין שרת משלנו.** האפליקציה סטטית לגמרי (GitHub Pages). מפתחות ה-AI נשמרים
   במכשיר ונשלחים ישירות לספק. אין backend, אין proxy, אין סודות בריפו.
4. **עובד אופליין.** PWA עם Workbox — כל התוכן, ה-CSS וה-JS במטמון.
5. **מובייל אינו "דסקטופ צר".** ראה §11 ואת `MOBILE_UX_SPEC.md`.
6. **★ המנוע גנרי, התוכן הוא נתון.** הסכימה ב-`types/content.ts` היא חוזה בין
   *יוצר תוכן* (היום: אנחנו; מחר: AI) לבין *מנוע הרינדור*. שמור על ההפרדה הזו
   נקייה — אל תערבב לוגיקה ספציפית לקורס C++ לתוך המנוע.

---

## 2. סטאק טכנולוגי ומפת קבצים

### הסטאק

| רכיב | בחירה | הערה |
|---|---|---|
| Framework | React 18 + TypeScript 5.7 (strict) | ללא Redux/Zustand — state ב-`App.tsx` + Context |
| Build | Vite 6 | `base` משתנה לפי `GITHUB_PAGES=1` |
| עיצוב | CSS גולמי אחד — `src/styles/global.css` | **אין Tailwind, אין CSS-in-JS, אין CSS Modules** |
| עורך קוד | Monaco (`@monaco-editor/react`) | נטען מה-bundle, לא מ-CDN (אופליין) |
| הרצת C++ | Wandbox API | `https://wandbox.org/api/compile.json` |
| AI | Anthropic SDK + fetch ידני ל-OpenAI/Gemini | streaming, ישירות מהדפדפן |
| Auth | Firebase Auth (Google) | popup בדפדפן, redirect ב-PWA מותקן |
| DB | Firestore — **מוגדר אך לא בשימוש** | ראה §13 |
| PWA | `vite-plugin-pwa` + Workbox | `registerType: 'prompt'` |
| ניתוב | **אין router.** state פנימי `{lessonId, sectionId}` | אין היסטוריית דפדפן, אין deep-link |

### מפת קבצים

```
.
├── AGENTS.md              ← אתה כאן
├── README.md              תיעוד למשתמש
├── MOBILE_UX_SPEC.md      מפרט מובייל מחייב
├── release.json           ★ גרסה + notes + forceUpdate (ידני!)
├── vite.config.ts         base path, PWA/manifest, version.json plugin, chunks
├── firebase.json / .firebaserc / firestore.rules
├── .github/workflows/deploy.yml   בנייה + פריסה ל-Pages בכל push ל-main
├── index.html             RTL, מטא-תגיות iOS, טעינת פונטים
├── public/                אייקונים (192/512/maskable/apple-touch/favicon)
└── src/
    ├── main.tsx           entry: Monaco setup, CSS, PWA init, window.__dev
    ├── App.tsx            ★ כל ה-state המרכזי, פריסה, ניווט, קיצורי מקלדת
    ├── AppContext.tsx     API לרכיבים: askAi / openIde / toast
    ├── types/content.ts   ★★ סכימת התוכן — גם החוזה שה-AI ייצר לתוכו (§6)
    ├── content/
    │   ├── index.ts       קטלוג 13 השיעורים (12 stubs) — סטטי, יהפוך ל-registry (§6.3 A)
    │   └── lesson01/      ~7,700 שורות תוכן, 12 פרקים
    ├── components/
    │   ├── Splash.tsx     מסך פתיחה + בחירת גוגל/אורח
    │   ├── Sidebar.tsx    תפריט שיעורים + חיפוש + התקדמות
    │   ├── Home.tsx       מסך הבית
    │   ├── BlockRenderer.tsx  ★ מפנה כל בלוק לרכיב שלו
    │   ├── CodeBlock.tsx  קוד סטטי + הרצה + WrapNotice + RunResult
    │   ├── CodeFileModal.tsx  תצוגת "קובץ מלא" (מובייל)
    │   ├── MemoryDiagram.tsx  Stack/Heap מונפש
    │   ├── Quiz.tsx  Exercise.tsx
    │   ├── Ide.tsx        ★ 760 שורות — סביבת פיתוח מלאה (דסקטופ בלבד)
    │   ├── AiPanel.tsx    ממשק הצ'אט (ה-state עצמו ב-App)
    │   ├── Settings.tsx   512 שורות — drill-down: חשבון/AI/תצוגה/מערכת
    │   ├── AuthButton.tsx  Icons.tsx
    ├── hooks/useAiChat.ts ★ state השיחה — חי מעל AiPanel בכוונה
    ├── lib/
    │   ├── compile.ts     Wandbox + stripCommentsAndStrings
    │   ├── wrap.ts        עטיפה חכמה של קטעים חלקיים ב-int main
    │   ├── ai.ts          ★ system prompt, בניית הקשר, בחירת מודל אוטומטית
    │   ├── providers/     types.ts (ממשק+SSE+שגיאות) · anthropic · openai · gemini
    │   ├── storage.ts     ★ localStorage: מפתחות, התקדמות, חידונים, פרויקטים
    │   ├── firebase.ts    auth + db (db לא בשימוש עדיין)
    │   ├── pwa.ts         SW + בדיקת version.json + עדכון כפוי
    │   ├── highlight.ts   הדגשת C++ לבלוקים סטטיים + לוג הקומפיילר
    │   ├── markdown.ts    renderInline / renderMarkdown (לתשובות ה-AI)
    │   ├── sectionText.ts המרת עמוד/בלוק לטקסט — ההקשר שנשלח ל-AI
    │   └── monacoSetup.ts ערכות נושא cpp-dark / cpp-light
    └── styles/global.css  ★ 3,373 שורות — כל העיצוב במקום אחד
```

הקבצים המסומנים ב-★ הם הקבצים שכמעט כל משימה תיגע בהם. קרא אותם לפני שינוי.

---

## 3. הרצה, בנייה ופריסה

```bash
npm install
npm run dev        # http://localhost:5173 (נפתח אוטומטית)
npm run build      # tsc -b && vite build   ← זו גם בדיקת הטיפוסים היחידה בפרויקט
npm run preview    # בדיקת ה-build המקומי
```

**חשוב לדעת:**

* **אין ESLint, אין Prettier, אין בדיקות.** `npm run build` (שכולל `tsc -b`
  ב-strict) הוא **שער האיכות היחיד**. אסור לסיים משימה בלי שהוא עובר.
* **ב-`npm run dev` אין Service Worker** (`devOptions.enabled: false`), ולכן אי
  אפשר לבדוק PWA/עדכונים/אופליין ב-dev. צריך `build` + `preview`.
* ב-dev בלבד נחשף `window.__dev` (`src/main.tsx`) — כלי לקמפול כל בלוקי הקוד
  של השיעור ולקבלת דוח כשלים. הסקריפט המלא נמצא ב-`README.md`.
* **פריסה אוטומטית:** כל push ל-`main` מפעיל את `.github/workflows/deploy.yml`
  שבונה עם `GITHUB_PAGES=1` ופורס ל-GitHub Pages. אין שלב אישור. אין CI על PR —
  **הבנייה מאומתת רק אחרי המיזוג**, ולכן חובה להריץ `npm run build` מקומית.
* כתובת האתר: `https://<user>.github.io/cpp-oop-course/` — `REPO_NAME`
  מקודד ב-`vite.config.ts`. שינוי שם הריפו מחייב עדכון שם.

---

## 4. ארכיטקטורה וזרימת המידע

### עץ הרכיבים

```
main.tsx
└── App.tsx  ← כל ה-state המרכזי
    ├── Splash            (עד שהמשתמש "נכנס")
    ├── Sidebar           ניווט
    ├── main.topbar       תפריט/בית/אופליין/פירורי לחם/IDE/ערכת נושא/Auth/הגדרות/AI
    ├── content-scroll
    │   ├── Home          כשאין route
    │   └── article.page  ← BlockRenderer לכל בלוק בעמוד
    ├── AiPanel           (מקבל state מ-useAiChat שחי ב-App)
    ├── ai-fab            כפתור צף
    ├── Settings          (modal)
    ├── Ide               (overlay מלא)
    ├── toast
    └── update-banner
```

### עקרונות state שחייבים להישמר

| כלל | למה | איפה |
|---|---|---|
| **כל ה-state המרכזי ב-`App.tsx`** | אין router ואין store; זה הרכיב היחיד שחי לאורך כל חיי האפליקציה | `App.tsx:41-80` |
| **שיחת ה-AI חיה ב-`useAiChat` שנקרא מ-`App`, לא בתוך `AiPanel`** | `AiPanel` עושה unmount בכל סגירה. אילו ה-state היה בתוכו, השיחה הייתה מתאפסת בכל ניווט — בניגוד למפרט | `hooks/useAiChat.ts`, `App.tsx:73` |
| **תקשורת מרכיבים כלפי מעלה רק דרך `AppCtx`** | `askAi` / `openIde` / `toast` — אין prop drilling | `AppContext.tsx` |
| **ניווט = `setRoute` + `setLastSection`** | אין URL. `getLastSection()` מחזיר את המשתמש לעמוד האחרון בפתיחה הבאה | `App.tsx:126-132` |
| **★ `LESSONS` מיובא ישירות ב-4 מקומות — זה חוב, לא תבנית** | כשיתווספו שיעורי משתמש שנוצרו מ-AI, מקור השיעורים חייב להיות דינמי. אל תוסיף ייבוא ישיר חמישי (§6.3 A) | `App.tsx` `Sidebar.tsx` `Home.tsx` `main.tsx` |

### הזרימה של "שאל את ה-AI על הקטע הזה"

```
BlockRenderer (כפתור ✦)
  → blockToText(block) + blockLabel(block)          [lib/sectionText.ts]
  → app.askAi(focusText, focusLabel)                [AppContext]
  → App: setAiFocus + setAiOpen(true)               [App.tsx:146]
  → aiPopup = aiOpen && focus  → מחלקת CSS 'ai-popup' (משנה התנהגות במובייל!)
  → AiPanel: send() → chat.send(q, {...ctx, focus}) [useAiChat]
  → askAI(): בונה system+הקשר, בוחר מודל, מזרים     [lib/ai.ts]
```

---

## 5. מערכת התוכן — הסכימה

כל התוכן **היום** הוא TypeScript טיפוסי, לא Markdown ולא JSON. הסכימה המלאה
מתועדת ב-`src/types/content.ts` — **זה המקור היחיד לאמת**.

```
Lesson → Chapter[] → Section[] → Block[]
```

> **★ שים לב לפני שאתה נוגע בסכימה:** הסכימה הזו אינה רק "איך אנחנו כותבים
> תוכן" — היא **פורמט היעד שה-AI יידרש לייצר לתוכו** ממצגות שהמשתמש יעלה
> (§6). לכן היא חייבת להישאר **JSON טהור** (בלי פונקציות/JSX/Date), וכל
> הרחבה חייבת להיות **אופציונלית** כדי לא לשבור תוכן שנוצר קודם. המבנה עצמו
> (`Lesson → Chapter → Section → Block`) הוא חוזה יציב — אל תשנה אותו בלי
> צורך אמיתי.

### סוגי הבלוקים הקיימים

| `kind` | מה זה | רכיב מרנדר |
|---|---|---|
| `heading` | כותרת בתוך עמוד (h2/h3/h4) | inline |
| `prose` | פסקה עם סימון מוטבע | `renderInline` |
| `list` | רשימה ממוספרת/לא | inline |
| `callout` | תיבה צבעונית: `info` `tip` `warn` `danger` `exam` `modern` `crefresh` `slide` | inline |
| `code` | קוד + הדגשות + הערות שורה + הרצה + פרויקט מרובה קבצים | `CodeBlock` |
| `compare` | שתי גרסאות קוד זו לצד זו עם verdict | inline |
| `memory` | דיאגרמת Stack/Heap מונפשת צעד-צעד | `MemoryDiagram` |
| `quiz` | רב-ברירה (גם multi) עם הסבר לכל תשובה | `Quiz` |
| `pitfall` | קוד שבור → שגיאת קומפיילר → תיקון | inline |
| `exercise` | תרגיל עם רמזים, פתרון ופלט צפוי | `Exercise` |
| `table` / `terms` / `steps` / `diagram` / `divider` | טבלה / מילון he-en / שלבים / SVG / קו | inline |

### סימון מוטבע (`renderInline`, `lib/markdown.ts`)

```
**מודגש**   *נטוי*   `קוד`   ==הדגשה צהובה==   [[מונח|הסבר בטולטיפ]]   [[מונח]]
```

התוצאה מוזרקת עם `dangerouslySetInnerHTML`. **התוכן כולו נכתב היום על ידינו
ואינו קלט משתמש** — זו ההנחה היחידה שמצדיקה את זה.

> **★ ההנחה הזו עומדת ליפול.** ברגע שהמשתמש מעלה מצגת וה-AI מייצר ממנה תוכן,
> ההזרקה הזו הופכת לפרצה. **sanitizer הוא תנאי סף לפיצ'ר הייבוא** — לא
> "שיפור עתידי". ראה §6.3 שורות D ו-E.

### שדות קריטיים בבלוק `code`

| שדה | מתי משתמשים |
|---|---|
| `runnable` | מוסיף כפתור "הרץ" (דסקטופ). בלעדיו הקוד סטטי |
| `files[]` | פרויקט מרובה קבצים — לשוניות + קומפילציה נפרדת אמיתית |
| `prelude` | הגדרות מעמוד קודם שהקטע נשען עליהן; נוספות בשקט לפני ההרצה ונראות ב"הצג את הקובץ המלא" |
| `noWrap` | **מבטל** עטיפה אוטומטית — לקטעים שאמורים להיכשל בכוונה |
| `broken` | מסגרת אדומה: הקוד אמור להיכשל |
| `highlight[]` / `annotations[]` | הדגשת שורות / הערות ממוספרות מתחת לקוד |

### הוספת שיעור חדש ידנית (2–13)

1. צור `src/content/lessonNN/` עם קבצי פרקים שמייצאים `Chapter`.
2. צור `index.ts` שמייצא `Lesson` עם `ready: true`.
3. החלף את ה-`stub(...)` המתאים ב-`src/content/index.ts` בייבוא האמיתי.
4. הרץ את סקריפט הבדיקה מ-`README.md` בקונסולה — כל בלוק `runnable` חייב
   להתקמפל בפועל.

**קונבנציות תוכן שחייבים לשמר:** `id` של Section ייחודי בתוך השיעור (הוא
מפתח ההתקדמות ב-localStorage — שינוי id מוחק התקדמות של משתמשים!), `slides[]`
מפנה לשקפים במצגת המקורית, ולכל `quiz`/`exercise` יש `id` יציב.

> זהו **המסלול הידני**, והוא יישאר תקף לתמיד לשיעורי הליבה. במקביל אמור לקום
> **מסלול שני** — שיעור שנוצר על ידי AI ממצגת של המשתמש, שאינו עובר דרך
> ה-bundle בכלל. שני המסלולים מייצרים בסוף **אותו מבנה `Lesson` בדיוק**, וזו
> בדיוק הנקודה. ראה §6.

---

## 6. ★ ייבוא מצגות משתמש — החזון והשלכותיו על הקוד

**זה הפרק החשוב ביותר במסמך לתכנון משימות חדשות.**

### 6.1 מה המוצר אמור לעשות

```
המשתמש מעלה מצגת (PDF / PPTX)
        ↓
חילוץ טקסט, קוד ומבנה מהשקפים
        ↓
ה-AI בונה ממנה Lesson מלא לפי הסכימה של types/content.ts:
  פרקים → עמודים → בלוקים (prose, code, callout, quiz, memory, exercise…)
        ↓
ולידציה + תיקון עצמי של הפלט
        ↓
השיעור נשמר, מופיע בסיידבר כמו כל שיעור אחר, ו**נראה ומתנהג זהה לחלוטין**
לשיעורים שכתובים היום ידנית — כולל הרצת קוד, דיאגרמות זיכרון וחידונים
```

**מבחן ההצלחה:** סטודנט שנכנס לשיעור שנוצר מהמצגת שלו לא אמור להיות מסוגל
לזהות שהוא לא נכתב ביד.

### 6.2 מה כבר עובד לטובת החזון (אל תשבור!)

הארכיטקטורה הנוכחית מוכנה לזה במידה מפתיעה. הנכסים האלה **קריטיים** — אל תפגע בהם:

| נכס | למה זה קריטי לייבוא |
|---|---|
| **הסכימה היא data טהור** — `Block` הוא union של אובייקטים פשוטים, בלי פונקציות, בלי JSX, בלי מחלקות | אפשר לייצר אותה כ-JSON ישירות מ-LLM. **אל תוסיף לסכימה שדה שאינו JSON-serializable — זה יהרוס את היכולת לייצר תוכן** |
| **הפרדה מלאה בין תוכן לרינדור** — `BlockRenderer` לא יודע כלום על C++ | אותו מנוע ירנדר כל מצגת בכל נושא |
| **`sectionText.ts`** ממיר כל בלוק לטקסט | כבר עכשיו פועל על בלוק שרירותי — יעבוד גם על מיובא |
| **`wrap.ts` + `compile.ts`** עובדים על קוד שרירותי | קוד ממצגת חדשה ירוץ בלי שינוי |
| **שכבת ספקי ה-AI מופשטת** (`Provider` interface) | הייצור יוכל לרוץ על כל אחד משלושת הספקים |
| **המשתמש מביא מפתח משלו** | עלות הייצור על המשתמש, לא עלינו — מודל בר-קיימא בלי backend |
| **`flattenLesson` / `countSections`** גנריים | עובדים על כל `Lesson`, מאיפה שלא בא |

### 6.3 מה חוסם היום — ההנחות שצריך לפרק

כל אחד מאלה הוא הנחת "התוכן שלנו וידוע מראש" שתישבר ברגע שהתוכן מיוצר:

| # | ההנחה | איפה בקוד | מה צריך במקום |
|---|---|---|---|
| A | **`LESSONS` הוא מערך סטטי** שמיובא ישירות ב-4 מקומות | `content/index.ts`, `App.tsx:2`, `Sidebar.tsx:2`, `Home.tsx:1`, `main.tsx:6` | רישום (registry) שממזג שיעורים מובנים + שיעורי משתמש, ומסופק דרך Context/hook. **אל תוסיף עוד `import { LESSONS }` ישיר** |
| B | **הרינדור מניח תוכן תקין** — ה-`switch` ב-`BlockRenderer` וב-`blockToText` **חסר `default:`** ונשען על exhaustiveness של TypeScript | `BlockRenderer.tsx`, `sectionText.ts` | זה מספיק לתוכן מהודר, אבל בלוק עם `kind` לא מוכר מ-AI פשוט **ייעלם בשקט**. צריך `default:` שמרנדר fallback גלוי |
| C | **אין ולידציה בזמן ריצה** לסכימה — הבטחון היחיד הוא `tsc` | — | ולידטור runtime לכל `Block` (ידני או zod), שמדווח מה נפסל ומאפשר תיקון חוזר מול ה-AI |
| D | **`dangerouslySetInnerHTML` בכל מקום**, בהצדקה "התוכן שלנו" | `markdown.ts` וכל צרכניו | ההצדקה **מתבטלת** ברגע שהתוכן מיוצר/מיובא. חובה sanitizer לפני שהפיצ'ר עולה |
| E | **בלוק `diagram` מקבל SVG גולמי** ומזריק אותו כמו שהוא | `BlockRenderer.tsx` (case `'diagram'`) | וקטור הזרקה ישיר בתוכן מיוצר. חובה סינון/whitelist של תגיות SVG |
| F | **`id` של Section הוא מפתח ההתקדמות הגלובלי** | `storage.ts`, `App.tsx` | שיעורי משתמש חייבים namespace (למשל `u:<uid>:<slug>`) כדי שלא יתנגשו עם שיעורים מובנים ולא ימחקו התקדמות |
| G | **התוכן חלק מה-bundle** ונשמר במטמון ה-PWA דרך `globPatterns` | `vite.config.ts` | שיעור שנוצר בזמן ריצה לא נמצא ב-bundle — צריך אסטרטגיית אופליין נפרדת |
| H | **`localStorage` לאחסון** (מגבלה ~5MB) | `storage.ts` | שיעור אחד הוא כמה מאות KB. לכמה שיעורי משתמש צריך IndexedDB (מקומי) ו/או Firestore (סנכרון) |
| I | **שכבת ה-AI היא צ'אט בלבד** — `askAI` מזרים טקסט לפאנל | `lib/ai.ts` | ייצור דורש נתיב אחר: פלט מובנה (JSON), עיבוד בנתחים (מצגת של 70 שקפים לא נכנסת לבקשה אחת), התקדמות, ניסיון חוזר, ותקציב טוקנים |
| J | **`SYSTEM` prompt ספציפי לקורס** ("אתה מתרגל בקורס תכנות מתקדם ב-C++") | `lib/ai.ts` | prompt הייצור הוא עניין נפרד לגמרי. **אל תעמיס אותו על ה-SYSTEM של הצ'אט** |
| K | **`modelPrefs` מכוונים לצ'אט** | `providers/*.ts` | לייצור צריך העדפות אחרות (חלון הקשר גדול, פלט מובנה) |
| L | **אין parser ל-PDF/PPTX** בפרויקט | — | תלות חדשה שתידרש. שקול משקל bundle והשפעה על אופליין |
| M | **`stub()` ו-`ready: boolean`** מניחים "שיעור שנכתב ידנית או לא" | `content/index.ts`, `types/content.ts` | צריך מקור לשיעור: `'builtin' \| 'user'`, ומטא-דאטה (מי יצר, מתי, מאיזו מצגת) |

### 6.4 כללי עבודה שנגזרים מזה — תקפים כבר עכשיו

גם אם המשימה שלך לא נוגעת בייבוא בכלל:

1. **הסכימה ב-`types/content.ts` היא ממשק ציבורי.** שינוי בה = שינוי בחוזה שה-AI
   יידרש לייצר לפיו. הרחבות תמיד **תוספתיות ואופציונליות** (`?`), לעולם לא
   שדה חובה חדש בבלוק קיים — זה ישבור כל תוכן שנוצר קודם.
2. **אל תוסיף לסכימה שדה שאינו JSON.** בלי פונקציות, בלי `ReactNode`,
   בלי `Date`, בלי מחלקות. מחרוזות, מספרים, בוליאנים, מערכים ואובייקטים בלבד.
3. **אל תכניס לוגיקה ספציפית ל-C++ לתוך רכיבי המנוע.** `BlockRenderer`,
   `Quiz`, `MemoryDiagram` חייבים להישאר אגנוסטיים לנושא. לוגיקה ספציפית
   לשפה מקומה ב-`wrap.ts`/`compile.ts`/`highlight.ts` בלבד.
4. **כשאתה מוסיף רכיב שקורא תוכן — הנח שהוא עלול להיות פגום.** שדה חסר,
   מערך ריק, `kind` לא מוכר. אל תסמוך על טיפוסים בלבד.
5. **אל תוסיף `import { LESSONS }` ישיר חדש.** ארבעת הקיימים הם חוב שצריך
   לפרוק, לא תבנית לחיקוי.
6. **כשאתה כותב תוכן ידנית לשיעורים 2–13 — אתה בעצם כותב את מערך האימון
   והייחוס לייצור האוטומטי.** שמור על עקביות מוחלטת בשימוש בבלוקים; חוסר
   עקביות בתוכן הידני יתורגם לחוסר עקביות בתוכן המיוצר.

### 6.5 סדר בנייה מוצע (כשמגיעים לזה)

לא לבנות הכול בבת אחת. הסדר שממזער סיכון:

```
1. ולידטור runtime לסכימה + default: ברינדור      ← לא שובר כלום, מגן על הכול
2. sanitizer ל-HTML/SVG                            ← חובה לפני כל תוכן חיצוני
3. registry דינמי ל-LESSONS + namespace ל-id       ← מפרק את ההנחה הסטטית
4. אחסון (IndexedDB) + סנכרון Firestore            ← תלוי בסנכרון שעדיין לא נכתב
5. נתיב ייצור מובנה בשכבת ה-AI (בלי UI)            ← לבדוק על טקסט מודבק ידנית
6. ingestion של PDF/PPTX                           ← החלק הכי "מלוכלך", אחרון
7. UI: העלאה, התקדמות, עריכה ואישור של התוצאה
```

**שלבים 1–2 שווים כבר עכשיו** — הם משפרים את החוסן גם בלי הפיצ'ר.

### 6.6 מה זה אומר על התוכן הידני הקיים

שיעור 1 הוא **המפרט החי** של "איך שיעור טוב נראה". כשה-AI ייצר שיעור, ההשוואה
תהיה מולו. לכן:

* אל "תפשט" בלוקים קיימים כדי לחסוך שורות — הצפיפות והמגוון הם המפרט.
* שמור על יחס בריא בין סוגי בלוקים בכל פרק (הסבר → קוד → מלכודת → חידון).
* כל דפוס חדש שאתה ממציא בתוכן — תעד אותו כאן, כדי שהייצור יוכל לחקות אותו.

---

## 7. מנוע ההרצה: Wandbox + עטיפה חכמה

### `lib/compile.ts`

* נקודת כניסה: `runProject(files, {stdin, signal})`.
* בוחר את הקובץ עם `int main(` כקובץ הראשי; כל שאר קבצי ה-`.cpp` נשלחים
  ב-`codes[]` **וגם** נוספים ל-`compiler-option-raw` — בלי זה הם לא יקושרו.
* Wandbox קורא לקובץ הראשי `prog.cc`; אנחנו מחליפים את השם חזרה בהודעות
  הקומפיילר כדי שהסטודנט יראה את שמו האמיתי.
* מפריד בין כשל קומפילציה לכשל **לינקר** (`undefined reference`,
  `multiple definition`, `collect2`) — זו הבחנה לימודית מרכזית בקורס.
* `stripCommentsAndStrings()` — **שומר על אורך זהה ועל מעברי שורה** כדי
  שאינדקסים מהסריקה יהיו תקפים על המקור. אל תשנה את החוזה הזה; `wrap.ts` תלוי בו.

### `lib/wrap.ts`

הרבה שקפים במצגת אינם תוכנית שלמה. `wrapSnippet` / `wrapProject`:

1. מפצלים לקטעים ברמה העליונה ומסווגים: `directive` / `declaration` / `statement`.
2. הגדרות נשארות ב-file scope, פקודות נכנסות ל-`int main()`.
3. מוסיפים `#include` חסרים לפי `LIB_RULES` ו-`using namespace std;`.
4. מחזירים `notes[]` — **מה בדיוק הוספנו** — שמוצג למשתמש ב-`WrapNotice`.

**זה לא קוסמטי — זו הבטחת שקיפות למשתמש.** אם אתה משנה את הלוגיקה, עדכן גם את
`notes` כדי שהמשתמש ימשיך לראות אמת.

### מגבלות ידועות

* דורש אינטרנט. אין fallback מקומי.
* אין הגבלת קצב/תור מצדנו — לחיצות מרובות = בקשות מרובות ל-Wandbox.
* `CodeBlock.run()` **אינו** מעביר `AbortSignal` (ב-`Ide` כן יש ביטול).

---

## 8. שכבת ה-AI (שלושה ספקים)

### מבנה

```
lib/ai.ts            SYSTEM prompt, buildContextBlock, askAI, resolveWorkingModel, checkProvider
lib/providers/
  types.ts           ממשק Provider אחיד + readSSE + describeHttpError + rankModels
  anthropic.ts       Anthropic SDK, dangerouslyAllowBrowser: true
  openai.ts          fetch + SSE
  gemini.ts          fetch + SSE
```

### כללים שאסור לשבור

1. **בחירת המודל אוטומטית לגמרי — אין בורר מודלים, בכוונה.** ספקים משאירים
   ברשימת המודלים מודלים שהוצאו משימוש (Google מחזירה עליהם 404
   "no longer available to new users"). לכן `resolveWorkingModel` שואל את הספק
   אילו מודלים זמינים, מדרג לפי `modelPrefs`, **ומנסה עד 6 מהם בבקשת probe זעירה**
   עד שאחד באמת עונה. אל תחזיר בורר ידני ואל תקבע מודל קשיח.
2. **מודל שמור שהתיישן מזוהה ומוחלף בשקט** — `isStaleModel()` ב-`ai.ts` תופס
   שגיאה מתאימה, מנקה את המודל השמור ומנסה שוב פעם אחת בלבד.
3. **אין בדיקת פורמט מפתח חוסמת.** `looksLikeValidKey` היא רמז רך בלבד;
   `keyPrefix` מוגדר רק לספקים עם פורמט יציב (לא ל-Google — מפתחות חדשים
   מתחילים ב-`AQ.` ולא ב-`AIza`).
4. **שגיאות מתורגמות לעברית מובנת** ב-`describeHttpError` — כולל זיהוי לפי
   *תוכן* ולא רק לפי קוד HTTP (Google מחזירה 400 על מפתח לא תקין, לא 401).
5. **ההקשר נשלח פעם אחת בתחילת השיחה** (`askAI`), מוגבל ל-12,000 תווים לעמוד
   ו-4,000 למיקוד. אל תשלח את ההקשר בכל תור — זה מכפיל עלות למשתמש.
6. **ה-SYSTEM prompt הוא החלטת מוצר**, לא טקסט חופשי: עברית, גוף שני, מונחים
   מקצועיים באנגלית, סגנון הקורס לפני C++ מודרני, בלי "שאלה מצוינת!". שינוי בו
   משנה את חוויית כל הסטודנטים — אל תשנה בלי בקשה מפורשת.
7. **★ ייצור תוכן ממצגת הוא נתיב נפרד, לא הרחבה של הצ'אט.** השכבה הזו בנויה
   כולה לצ'אט מזרים: `askAI` מחזיר טקסט חופשי ל-UI, ה-`SYSTEM` ספציפי לקורס,
   וה-`modelPrefs` מכוונים לשיחה. ייצור דורש פלט מובנה, נתחים, ולידציה
   וניסיון חוזר. **אל תעמיס את זה על `askAI` ועל ה-`SYSTEM` הקיימים** — בנה
   מודול ייצור לצדם, שמשתמש באותו `Provider` interface. ראה §6.3 שורות I–K.

### אבטחה — המודל הנוכחי

המפתחות נשמרים **רק ב-localStorage** ונשלחים **ישירות** לספק מהדפדפן. אין שרת
ביניים ואין proxy. זו החלטה מודעת (אין backend), והיא מתועדת למשתמש ב-README.
המשמעות: כל סקריפט צד-שלישי בעמוד היה יכול לקרוא אותם — ולכן **אסור להוסיף
סקריפטים חיצוניים לעמוד**.

---

## 9. התחברות, אחסון ו-PWA

### Firebase Auth

| מצב | שיטת התחברות | למה |
|---|---|---|
| דפדפן רגיל | `signInWithPopup` | לא עוזב את הדף |
| PWA מותקן (`isStandalone()`) | `signInWithRedirect` | popup נחסם/לא אמין ב-iOS standalone |

`consumeRedirectResult()` נקרא פעם אחת מ-`Splash`. **הוא לא בולע שגיאות בכוונה** —
`Splash` חייב להציג אותן. באג היסטורי: כישלון redirect באייפון נראה כמו "מסך
שחור" בלי הסבר. יש גם מרוץ שנפתר: `redirectChecked` חייב להסתיים לפני שמחליטים
להציג את מסך הבחירה, אחרת `onAuthChange` יורה `null` באמצע התחברות אמיתית.

### `lib/storage.ts` — כלל ההתקדמות

```
isSignedIn() === Boolean(auth.currentUser)
```

**רק משתמש מחובר שומר התקדמות.** אורח מתחיל מאפס בכל כניסה — בכוונה: בלי זה
"אורח" היה מסך פתיחה קוסמטי בלבד. **לא** חל על: מפתחות API, ספק, מודל, ערכת
נושא, גודל גופן, גרסת אפליקציה, בחירת כניסה — אלה הגדרות מכשיר.

מפתחות localStorage (כולם בקידומת `cppcourse.`): `provider` `keys` `models`
`projects` `appVersion` `completed` `quiz` `scratch` `exercises.<id>` `theme`
`lastSection` `fontScale` `entryChoice`.

`resetAllProgress()` מוחק הכול **חוץ מ**־`keys` `provider` `models` `theme`
`appVersion` `entryChoice`.

### PWA וגרסאות

שני מנגנונים משלימים ב-`lib/pwa.ts`:

1. **Workbox** מודיע כש-Service Worker חדש כבר הורד וממתין (`onNeedRefresh`).
2. **`version.json`** — נבנה מ-`release.json` על ידי plugin ב-`vite.config.ts`,
   ונקרא **תמיד מהרשת, לעולם לא מהמטמון** (`NetworkOnly` + `cache: 'no-store'`).

שניהם מובילים ל-`applyUpdate()`: מסך טעינה ממותג → החלפת SW → `location.reload()`.
יש `Promise.race` עם timeout של 6 שניות כי `updateSW(true)` נתקע לנצח אם אין SW
ממתין.

> **★ כלל תפעולי:** `release.json` מתעדכן **ידנית בלבד**. הבאנר "גרסה חדשה
> זמינה" מופיע רק כשמעלים שם את `version` — לא בכל push. `forceUpdate: true`
> מרענן את כל המשתמשים בלי לשאול. **אל תעלה גרסה בלי בקשה מפורשת, ואל תיגע
> ב-`forceUpdate` בלי אישור.**

### Firestore

`firestore.rules` פרוס ותקין (owner-only: `request.auth.uid == userId`),
`db` מיוצא מ-`firebase.ts` — **אבל אף שורת קוד לא קוראת או כותבת אליו.**
הסנכרון עצמו לא נכתב. ראה §13.

---

## 10. מערכת העיצוב

### הכלל הראשון

**כל העיצוב נמצא ב-`src/styles/global.css` (3,373 שורות).** אין Tailwind, אין
`styled-components`, אין CSS Modules. `style={{}}` inline מותר רק לערכים
דינמיים חד-פעמיים. **אל תכניס ספריית עיצוב לפרויקט.**

### טוקנים

הכול דרך CSS variables ב-`:root`, ומוחלף ב-`:root[data-theme='dark']`
(נקבע ב-`App.tsx:110` על `documentElement`).

| קבוצה | משתנים |
|---|---|
| רקעים | `--bg` `--bg-elev` `--bg-sunken` `--bg-code` `--bg-code-hl` |
| טקסט | `--fg` `--fg-soft` `--fg-faint` |
| גבולות | `--border` `--border-strong` |
| מותג | `--brand` `--brand-soft` `--brand-strong` (`#3d4ec7`) |
| סמנטיים | `--ok` `--warn` `--err` `--info` `--purple` `--accent` + `-soft` לכל אחד |
| קוד | `--tk-kw` `--tk-type` `--tk-str` `--tk-num` `--tk-com` `--tk-fn` `--tk-pre` `--tk-op` `--tk-punc` `--tk-bi` `--code-fg` |
| מידות | `--r-sm: 6px` `--r: 10px` `--r-lg: 16px` `--sidebar-w: 320px` `--ai-w: 440px` |
| צללים | `--shadow-sm` `--shadow` `--shadow-lg` |
| גופן | `--fs` (מכפיל גודל, נשלט מההגדרות) |

**אסור לכתוב צבע קשיח (hex) ברכיב חדש.** אם חסר צבע — הוסף טוקן לשתי הערכות.
בלוק הקוד תמיד כהה בשתי הערכות, בכוונה.

### טיפוגרפיה

* עברית: `Heebo` (Google Fonts, משקלים 300–800).
* קוד: `JetBrains Mono`.
* `body` = `calc(16px * var(--fs))`, `line-height: 1.75`.

### RTL — הכלל החשוב ביותר בעיצוב

האפליקציה **תמיד** RTL (`<html dir="rtl">`). לכן:

* ברירת מחדל: השתמש בתכונות לוגיות — `margin-inline-start`, `padding-inline`,
  `border-inline-end`, `inset-inline`.
* **חריגים מכוונים** (מתועדים בקוד, אל תתקן אותם):
  * `.sidebar` במובייל משתמש ב-`right: 0` **פיזי** — `inset-inline-end` ב-RTL
    הוא בעצם `left`, מה שהעיף את הסיידבר לצד ההפוך מכפתור התפריט.
  * `.sidebar-close` משתמש ב-`left: 12px` פיזי — הקצה הפנימי של המגירה.
  * `.ide-editor` כופה `direction: ltr` — Monaco מחשב מיקומים בהנחת LTR ובלי
    זה מרונדר מחוץ למסך.
  * חצי מקלדת: **`←` = העמוד הבא** (`App.tsx:171`), וכך גם ב-`MemoryDiagram`.

### מוסכמות שמות CSS

שטוח וקצר, בלי BEM מלא: `.codeblock` `.code-head` `.code-act` `.code-tab`,
`.ai-panel` `.ai-head` `.ai-scroll`, `.ide-body` `.ide-side`,
`.callout.c-<variant>`, מצבים כ-`.on` / `.active` / `.broken`.

---

## 11. מובייל מול דפדפן — ההבדלים המלאים

> העיקרון מ-`MOBILE_UX_SPEC.md`: **"בגרסת מובייל הכול חייב להיראות שונה לגמרי"** —
> לא לדחוס את פריסת הדסקטופ לרוחב צר.
>
> חובת בדיקה: כל שינוי מובייל נבדק **באמולציית מובייל אמיתית בדפדפן**, לא רק
> בקריאת CSS.

### נקודות שבירה

| Breakpoint | מה משתנה |
|---|---|
| `max-width: 1100px` | כפתור ה-✦ שליד כל בלוק הופך ל-`static` וקבוע-נראה (אין hover במגע) |
| `max-width: 1000px` | פריסת ה-IDE מעמודות לשורות |
| `max-width: 980px` | **★ נקודת השבירה הראשית — כל המעבר למובייל** |
| `max-width: 900px` | דיאגרמת הזיכרון: קוד וויזואליזציה זה מתחת לזה |
| `max-width: 780px` | בלוק `compare` לעמודה אחת |
| `max-width: 700px` | כפתור ההתחברות מאבד את התווית, נשאר אייקון |
| `max-width: 560px` | רשת בחירת ספקי ה-AI לעמודה אחת |
| `@media print` | מסתיר ניווט/AI/פאנלים, פורס את התוכן לרוחב מלא |

### טבלת ההבדלים המלאה (מתחת ל-980px)

| נושא | דפדפן / דסקטופ | מובייל | איפה |
|---|---|---|---|
| **פריסה** | Grid: `sidebar 320px \| תוכן \| ai 440px` | עמודה אחת (`1fr`) | `global.css:168`, `3234-3237` |
| **סיידבר** | עמודה קבועה בפריסה | מגירה `position: fixed` מימין, `translateX`, עם backdrop כהה | `3245-3285` |
| **סגירת סיידבר** | נשאר פתוח | נסגר אוטומטית בכל ניווט | `App.tsx:130` |
| **עורך קוד (IDE)** | סביבת פיתוח מלאה: Monaco, ריבוי קבצים, הרצה, שמירה | **לא קיים כלל.** כל נקודות הכניסה מוסתרות (`.ide-desktop-only`, `.code-desktop-actions`) | `3289-3290` |
| **כפתור "הרץ"** | קיים בכל בלוק `runnable` | **לא קיים** — אין הרצה במובייל | `3290` |
| **צפייה בקוד מלא** | "פתח בעורך" | כפתור "הצג קובץ מלא" → `CodeFileModal`: **חלון גלילה במרכז המסך**, לא ניווט לעמוד | `CodeFileModal.tsx`, `3291` |
| **תרגילים** | כפתור "פתח בסביבת הפיתוח" | הערה מוסברת + רמזים + פתרון + "תכוון אותי" (AI) | `Exercise.tsx:57-77`, `3292` |
| **פאנל AI — פתיחה כללית** (FAB / סרגל עליון) | פאנל צד 440px | **מסך מלא** (`.main` מוסתר) | `3238` |
| **פאנל AI — מתוך שיעור** (✦ ליד בלוק) | אותו פאנל צד — זהה | **חלון קופץ ממורכז** מעל התוכן, עם backdrop; העמוד נשאר גלוי מאחור | `App.tsx:204`, `3295-3315` |
| **שיחת AI** | מתמידה בין ניווטים | זהה — מתמידה, מתאפסת רק בסגירת האפליקציה | `useAiChat.ts` |
| **הגדרות** | כרטיס מודאלי ממורכז | **מסך מלא** (`inset: 0`, בלי פינות מעוגלות) | `3318-3330` |
| **מסך הבית** | רשת כרטיסים, hero רחב | hero קומפקטי, `hero-stats` 2×2, **קרוסלת פיצ'רים גוללת אופקית** עם scroll-snap, כרטיסי שיעור בעמודה אחת | `3338-3365` |
| **Safe area** | לא רלוונטי | `env(safe-area-inset-top/bottom)` בכל סרגל עליון — Dynamic Island / notch | `413-420`, `3240`, `3256`, `3264`, `3327` |
| **התחברות Google** | popup | redirect (ב-PWA מותקן) | `firebase.ts:44-51` |
| **התקנה כאפליקציה** | אפשרי, פחות נפוץ | PWA מלא: `standalone`, `portrait-primary`, אייקון maskable | `vite.config.ts` |
| **כפתור התחברות** | אייקון + "התחברות" | אייקון בלבד (מתחת ל-700px) | `2920-2927` |
| **כפתור ✦ לכל בלוק** | מופיע ב-hover | קבוע ונראה תמיד (מתחת ל-1100px) | `586-591` |
| **קיצורי מקלדת** | `←` `→` `/` `B` `Ctrl+Enter` `Esc` | לא רלוונטיים | `App.tsx:165-178` |

### מה שעדיין לא מיושם מהמפרט

`MOBILE_UX_SPEC.md` דורש ש**"הכול ייראה שונה לגמרי"**. בפועל היום ה-JSX משותף
לשתי הגרסאות וההבדל הוא **CSS בלבד** (פלוס הסתרת נקודות כניסה). זה מספיק למסך
הבית, ההגדרות והסיידבר, אבל אינו "פריסה מובייל-נייטיבית" מלאה. הרחבות עתידיות
חייבות לכבד את המפרט. אם משימה דורשת שינוי JSX למובייל — קרא את המפרט קודם.

---

## 12. מה עובד היום

| יכולת | מצב | הערות |
|---|---|---|
| שיעור 1 — מחלקות | ✅ מלא | 12 פרקים, ~60 עמודים, ~7,700 שורות תוכן |
| קומפילציה נפרדת אמיתית | ✅ | g++ 13.2, שגיאות לינקר אמיתיות |
| עטיפה חכמה + שקיפות מלאה | ✅ | המשתמש רואה בדיוק מה נוסף |
| עורך מרובה-קבצים (Monaco) | ✅ דסקטופ | לשוניות, הוספה/מחיקה/שינוי שם, תבניות, `Ctrl+Enter` |
| שמירת פרויקטים/קטעים/קוד תרגילים | ✅ | localStorage, למשתמש מחובר בלבד |
| דיאגרמות זיכרון מונפשות | ✅ | Stack + Heap, ניגון אוטומטי, ניווט מקלדת |
| חידונים ותרגילים | ✅ | תשובות ומצב נשמרים |
| עוזר AI — 3 ספקים | ✅ | Claude / ChatGPT / Gemini, streaming, בחירת מודל אוטומטית |
| הקשר עמוד + מיקוד בלוק ל-AI | ✅ | `sectionText.ts` |
| התמדת שיחת AI בין ניווטים | ✅ | `useAiChat` |
| מצב כהה/בהיר + גודל גופן | ✅ | טוקנים מלאים בשתי הערכות |
| התחברות Google (popup + redirect) | ✅ | כולל טיפול בשגיאות ומרוץ ה-redirect |
| מצב אורח אמיתי | ✅ | לא נשמרת התקדמות, בכוונה |
| PWA: אופליין, התקנה, באנר עדכון, עדכון כפוי | ✅ | דורש `build`, לא עובד ב-dev |
| חיווי אופליין בסרגל העליון | ✅ | `online`/`offline` events |
| פריסה אוטומטית ל-GitHub Pages | ✅ | בכל push ל-`main` |
| כללי אבטחה של Firestore | ✅ פרוסים | אבל אף אחד לא כותב ל-DB עדיין |
| RTL מלא + safe-area (Dynamic Island) | ✅ | |
| חיפוש בסיידבר | ✅ | סינון לפי כותרות |
| הדפסה | ✅ | `@media print` |

---

## 13. מה חסר / לא גמור

מסודר לפי חשיבות. **לפני שאתה מתחיל משימה — בדוק אם היא כאן, ואם כן, עדכן את
השורה בסיום.**

### ★ הפיצ'ר המרכזי שטרם נבנה: ייבוא מצגות משתמש

**זה החוסר הגדול ביותר בפרויקט** — לא באג, אלא המוצר עצמו שעדיין לא קיים.
הפירוט המלא ב-§6; להלן הרכיבים כפריטי עבודה:

| # | רכיב חסר | מצב נוכחי | תלוי ב- |
|---|---|---|---|
| 0.1 | **ולידטור runtime לסכימה** | אין. הבטחון היחיד הוא `tsc` בזמן בנייה | — (אפשר כבר עכשיו) |
| 0.2 | **`default:` ברינדור לבלוק לא מוכר** | ה-`switch` ב-`BlockRenderer` וב-`blockToText` נשען על exhaustiveness; `kind` לא מוכר **נעלם בשקט** | — (אפשר כבר עכשיו) |
| 0.3 | **Sanitizer ל-HTML ול-SVG** | אין. `dangerouslySetInnerHTML` + SVG גולמי בבלוק `diagram` | תנאי סף לייבוא |
| 0.4 | **registry דינמי במקום `LESSONS` הסטטי** | מערך קבוע, מיובא ישירות ב-4 מקומות | 0.1 |
| 0.5 | **Namespace ל-`id` של שיעורי משתמש** | `id` שטוח; התנגשות תדרוס התקדמות | 0.4 |
| 0.6 | **אחסון שיעורים (IndexedDB / Firestore)** | `localStorage` בלבד, ~5MB | סנכרון Firestore (#1) |
| 0.7 | **נתיב ייצור מובנה בשכבת ה-AI** | `askAI` הוא צ'אט מזרים בלבד; אין פלט JSON, אין נתחים, אין retry | — |
| 0.8 | **prompt ייצור נפרד** | `SYSTEM` הנוכחי ספציפי לקורס C++ | 0.7 |
| 0.9 | **Ingestion של PDF/PPTX** | אין parser בפרויקט כלל | תלות חדשה |
| 0.10 | **UI: העלאה, התקדמות, עריכה ואישור** | אין | כל הנ"ל |
| 0.11 | **אסטרטגיית אופליין לשיעורי משתמש** | ה-PWA שומר במטמון רק את ה-bundle | 0.6 |

**0.1 ו-0.2 שווים לעשות כבר עכשיו** — הם מגנים גם על התוכן הידני הקיים.

### חוסרים מרכזיים

| # | מה חסר | מצב נוכחי | איפה | השפעה |
|---|---|---|---|---|
| 1 | **סנכרון Firestore בין מכשירים** | הכללים פרוסים, `db` מיוצא, **אפס קריאות/כתיבות בקוד**. שלושה מקומות בממשק מבטיחים "בקרוב" | `firebase.ts:31`, `AuthButton.tsx:69`, `Settings.tsx:227` | משתמש מחובר עדיין מאבד הכול במעבר מכשיר. זו ההבטחה הגדולה שלא מומשה |
| 2 | **שיעורים 2–13** | 12 `stub()` — מופיעים בתפריט כ"בקרוב", `ready: false`, בלי פרקים | `content/index.ts` | 92% מהקורס לא קיים (~471 שקפים). כפליים חשוב: התוכן הידני הוא גם **מפרט הייחוס** לייצור האוטומטי (§6.6) |
| 3 | **אין בדיקות בכלל** | אין vitest/jest, אין בדיקות רכיב, אין snapshot | — | כל רגרסיה מתגלה רק בייצור |
| 4 | **אין ESLint / Prettier** | `tsc -b` בלבד; `noUnusedLocals: false` | `tsconfig.json` | סגנון לא אחיד בין סוכנים שונים |
| 5 | **אין CI על PR** | ה-workflow רץ רק על push ל-`main` — כלומר **אחרי** המיזוג | `.github/workflows/deploy.yml` | build שבור מגיע לייצור. חובה להריץ build מקומי |
| 6 | **טופס משוב** | `FEEDBACK_FORM_URL = ''`, הכפתור `disabled` | `Settings.tsx:21,470` | אין ערוץ משוב מהסטודנטים |

### חוסרים בינוניים

| # | מה חסר | פירוט | איפה |
|---|---|---|---|
| 7 | **אין routing / deep-link** | ה-route הוא state פנימי. אי אפשר לשתף קישור לעמוד, כפתור "אחורה" של הדפדפן יוצא מהאפליקציה | `App.tsx:46` |
| 8 | **אין Error Boundary** | חריגה ברכיב אחד = מסך לבן לכל האפליקציה | `main.tsx` |
| ~~9~~ | ~~`sidebarOpen` לא מגיב ל-resize~~ **תוקן** | היה נקבע פעם אחת מ-`window.innerWidth > 980` בטעינה בלבד. עכשיו יש גם `resize` listener שמעדכן את המצב כשחוצים את נקודת השבירה 980px בפועל (לא בכל resize, כדי לא לדרוס פתיחה/סגירה ידנית באותו טווח) | `App.tsx` |
| 10 | **אין ביטול הרצה ב-`CodeBlock`** | `Ide` מעביר `AbortController`, `CodeBlock` לא | `CodeBlock.tsx:104` |
| 11 | **אין הגבלת קצב מול Wandbox** | לחיצות חוזרות = בקשות מקבילות | `compile.ts` |
| 12 | **`b` לפתיחת סיידבר לא עובד בפריסת מקלדת עברית** | `e.key === 'b'` מחזיר `נ` בעברית | `App.tsx:174` |
| 13 | **אין ניקוי מקומי ביציאה מהחשבון** | `signOut` מחזיר את הקריאות ל-`null`, אבל הנתונים נשארים ב-localStorage וחוזרים בהתחברות הבאה של **כל** משתמש באותו מכשיר | `storage.ts` |
| 14 | **פריסת מובייל = CSS בלבד** | ה-JSX משותף; המפרט דורש יותר מזה | §11 |
| 15 | **אין a11y מסודר** | אין `aria-label` עקבי, אין ניהול focus במודאלים, אין focus trap | כל הרכיבים |
| 16 | **`dangerouslySetInnerHTML` בכל מקום** | מוצדק **רק** כל עוד התוכן שלנו בלבד. ראה פריט 0.3 — זה הופך לחסם קשיח בייבוא | `markdown.ts` וצרכניו |

### מה **לא** חסר — אל "תתקן" את זה

* **אין בורר מודלים ידני** — החלטה מכוונת (§8).
* **אורח לא שומר התקדמות** — החלטה מכוונת (§9).
* **`release.json` ידני** — החלטה מכוונת (§9).
* **`right`/`left` פיזיים בסיידבר, `direction: ltr` ב-Monaco** — תיקוני באגים
  מכוונים ומתועדים (§14).
* **בלוק הקוד כהה גם בערכה הבהירה** — החלטת עיצוב.

---

## 14. מלכודות ידועות — אל תחזור עליהן

כל אחת מהן הייתה באג אמיתי שתוקן. ההערה בקוד מסבירה; **אל תמחק את ההערות**.

1. **ספציפיות CSS בסיידבר במובייל.** הכלל `@media (max-width: 980px)` חייב
   לכלול גם את `.app.sidebar-closed` ולא רק `.app` — אחרת הוא מפסיד בספציפיות,
   העמודה נשארת ברוחב 0, **וכל התוכן נעלם בטלפון**. `global.css:3231-3237`.
2. **RTL הופך `inset-inline`.** `inset-inline-end` בסיידבר העיף אותו לצד ההפוך
   מכפתור התפריט. הפתרון: `right` פיזי, מתועד. `global.css:3245`.
3. **Monaco מניח LTR.** בלי `direction: ltr` על `.ide-editor` הוא מרונדר מחוץ
   למסך. `global.css:2232-2234`.
4. **`updateSW(true)` נתקע לנצח** אם אין SW ממתין — `Promise.race` עם timeout.
   `pwa.ts:applyUpdate`.
5. **מרוץ ה-redirect בהתחברות iOS.** חובה לחכות ל-`consumeRedirectResult` לפני
   שמציגים את מסך הבחירה, אחרת `onAuthChange(null)` יורה ראשון והמשתמש רואה את
   מסך הבחירה שוב באמצע התחברות. `Splash.tsx`.
6. **`consumeRedirectResult` לא בולע שגיאות.** אם תעטוף אותו ב-`catch` ריק,
   כישלון התחברות באייפון יחזור להיראות כמו מסך שחור.
7. **`stripCommentsAndStrings` שומר אורך.** אם תשנה את זה, כל האינדקסים
   ב-`wrap.ts` יישברו.
8. **`version.json` לעולם לא מהמטמון.** יש לזה `NetworkOnly` + `no-store` —
   הוא מנגנון בדיקת העדכון עצמו.
9. **`id` של Section הוא מפתח ההתקדמות.** שינוי `id` של עמוד קיים מוחק את
   ההתקדמות של כל המשתמשים בו.
10. **סגירת פאנל ה-AI מנקה גם `focus`.** אחרת הפתיחה הכללית הבאה תיפתח בטעות
    כחלון קופץ במקום כפאנל צד. `App.tsx:155-158`.
11. **`manifest.id`/`scope`/`start_url` מוגבלים לתיקיית הריפו** — אחרת
    האפליקציה מתנגשת עם כל PWA אחר ב-`<user>.github.io`.
12. **בדפדפן, פאנל ה-AI נראה זהה בשני מצבי הפתיחה.** ההבדל popup/צד מוגבל
    ל-`max-width: 980px` בלבד. אל "תתקן" את זה בדסקטופ.
13. **`font-size` מתחת ל-16px על שדה קלט = זום אוטומטי ב-iOS Safari.** כל
    `input`/`textarea` שנגיש למובייל (חיפוש בסיידבר, קלט הצ'אט, מפתחות API
    בהגדרות) חייב `font-size` בפועל ≥16px, אחרת iOS מגדיל את כל הדף בפוקוס
    על השדה ולא תמיד מקטין בחזרה — נראה למשתמש כאילו "המסך קופץ" באמצע
    הקלדה. העיצוב הדסקטופי נשאר קומפקטי (0.85–0.92rem); התיקון הוא override
    ל-16px בתוך `@media (max-width: 980px)` בלבד. אם מוסיפים שדה קלט חדש
    שמשתמש עלול לגעת בו במובייל — הוסף אותו לרשימת הסלקטורים שם.
    `global.css:3287` (סוף הבלוק).

---

## 15. כללי עבודה בגיט לצוות מרובה סוכנים

> כמה בני אדם וכמה סוכנים עובדים על הריפו במקביל. הכללים האלה מונעים דריסות.

### ענפים

* **לעולם לא לפתח ישירות על `main`.** push ל-`main` = פריסה מיידית לייצור.
* כל משימה = ענף משלה. קונבנציה קיימת: `claude/<תיאור-קצר>-<hash>` לסוכנים.
  לבני אדם: `feat/…`, `fix/…`, `content/lesson02-…`.
* ענף אחד = משימה אחת. אל תערבב תיקון באג עם פיצ'ר עם תוכן.
* **אל תדחוף לענף של סוכן אחר** בלי תיאום.

### קומיטים

* הודעה בשורה אחת, בזמן ציווי, שמסבירה **למה** ולא רק מה. עברית ואנגלית שתיהן
  מקובלות בהיסטוריה — היה עקבי בתוך אותה סדרה.
* אל תכניס שינויי עיצוב לא קשורים לקומיט של לוגיקה — זה הופך code review
  לבלתי אפשרי כשכמה אנשים נוגעים ב-`global.css`.
* אל תזכיר שמות מודלים/כלים בהודעות הקומיט.

### הימנעות מקונפליקטים (חשוב במיוחד כאן)

שלושה קבצים הם "נקודות חמות" שכל סוכן נוגע בהן:

| קובץ | סיכון | איך מתגוננים |
|---|---|---|
| `src/styles/global.css` | 3,373 שורות, כולם עורכים | הוסף כללים **בסוף המקטע הרלוונטי**, לא באמצע. אל תסדר מחדש קטעים קיימים |
| `src/App.tsx` | כל ה-state | הוסף state חדש ליד קבוצת ה-state הדומה לו, לא בראש הקובץ |
| `release.json` | קובץ זעיר, קונפליקט ודאי | **אל תיגע בו** אלא אם המשימה היא שחרור גרסה |

כללים נוספים:
* עשה `git fetch origin main` ומזג/rebase לפני שאתה מתחיל וכשאתה מסיים.
* קובץ תוכן חדש (`lessonNN/chX.ts`) עדיף על עריכת קובץ קיים — פחות התנגשויות.
* **אל תריץ פורמט אוטומטי על קבצים שלא נגעת בהם.** אין Prettier בפרויקט, וריפורמט
  גורף יהפוך כל PR מקביל לקונפליקט ענק.

### מה אסור להעלות לריפו

* `.env` וכל מפתח אמיתי (כבר ב-`.gitignore`). ה-`apiKey` של Firebase
  ב-`firebase.ts` **אינו** סוד — זה מזהה קליינט ציבורי; ההגנה היא ב-Auth
  וב-Security Rules.
* `node_modules`, `dist`, `tsconfig.tsbuildinfo` (זה כרגע כן בריפו — אל תוסיף
  עוד קבצי build).
* קבצים זמניים: הקידומת `.tmp-*` כבר מסוננת.

### Pull Requests

* **אל תפתח PR אלא אם התבקשת במפורש.**
* אם פתחת: תאר מה השתנה, מה נבדק, ומה **לא** נבדק. אין PR template בריפו.
* אין CI על PR — כתוב במפורש בגוף ה-PR ש-`npm run build` עבר מקומית.

---

## 16. צ'קליסט לפני סיום משימה

- [ ] `npm run build` עובר נקי (זו בדיקת הטיפוסים היחידה).
- [ ] **★ מבחן החזון:** השינוי שלי עובד גם על שיעור ש**נוצר על ידי AI בזמן
      ריצה**, לא רק על התוכן המהודר שלנו? לא הנחתי שהתוכן שלנו, שהוא תקין, שהוא
      ידוע בזמן קומפילציה, או שיש בדיוק 13 שיעורים? (§6)
- [ ] אם נגעתי ב-`types/content.ts` — ההרחבה אופציונלית, JSON-serializable,
      ולא שוברת תוכן שנוצר קודם.
- [ ] לא הוספתי `import { LESSONS }` ישיר חדש, ולא הכנסתי לוגיקה ספציפית
      ל-C++ לתוך רכיבי המנוע (`BlockRenderer` / `Quiz` / `MemoryDiagram`).
- [ ] בדקתי בדפדפן **וגם** באמולציית מובייל אמיתית (≤980px) — לא רק קראתי CSS.
- [ ] בדקתי במצב כהה **וגם** בהיר.
- [ ] לא הכנסתי צבע hex קשיח — רק טוקנים.
- [ ] לא שברתי RTL; השתמשתי בתכונות לוגיות פרט לחריגים המתועדים.
- [ ] אם נגעתי בהרצת קוד — בדקתי גם קובץ יחיד וגם פרויקט מרובה קבצים, וגם
      מקרה שאמור להיכשל (`broken` / `noWrap`).
- [ ] אם נגעתי ב-AI — בדקתי לפחות ספק אחד מקצה לקצה, כולל מקרה בלי מפתח.
- [ ] אם נגעתי ב-storage — בדקתי גם כמשתמש מחובר וגם כאורח.
- [ ] לא נגעתי ב-`release.json` (אלא אם המשימה היא שחרור).
- [ ] לא מחקתי הערות הסבר קיימות בקוד — הן מתעדות באגים שתוקנו.
- [ ] **עדכנתי את `AGENTS.md`** אם שיניתי משהו שמתועד בו — במיוחד §12 ו-§13.
- [ ] הקומיט על הענף הנכון, לא על `main`.

---

## נספח: מונחים בקוד

| מונח | פירוש |
|---|---|
| **Section / עמוד** | יחידת הלימוד הבסיסית. יחידת הניווט וההתקדמות |
| **Block / בלוק** | פריט תוכן בתוך עמוד. `Block` union ב-`types/content.ts` |
| **focus / מיקוד** | הבלוק הספציפי שנשלח ל-AI בלחיצה על ✦ |
| **prelude** | קוד מעמוד קודם שנוסף בשקט לפני ההרצה |
| **wrap / עטיפה** | השלמת קטע חלקי לתוכנית שרצה |
| **stub** | שיעור שטרם נכתב, `ready: false` |
| **builtin lesson** | שיעור שכתוב בקוד ומהודר ל-bundle (כל מה שקיים היום) |
| **user lesson** | שיעור שנוצר מ-AI ממצגת שהמשתמש העלה — עדיין לא ממומש (§6) |
| **המנוע** | כל מה שמרנדר ומריץ תוכן ואינו קשור לנושא מסוים: `BlockRenderer`, `Quiz`, `MemoryDiagram`, `Ide` |
| **חוזה הסכימה** | `types/content.ts` כפורמט יעד לייצור אוטומטי, לא רק כטיפוסים |
| **standalone** | האפליקציה רצה כ-PWA מותקן, לא בטאב דפדפן |
| **entryChoice** | בחירת המשתמש במסך הפתיחה: `'guest'` או התחברות |

---

*מסמך זה נכתב ב-2026-08-14 ומתאר את מצב הריפו בנקודה הזו. **הוא חלק מהקוד** —
כל שינוי מהותי בארכיטקטורה, בעיצוב, ברשימת מה שעובד או במה שחסר, מחייב עדכון
של המסמך באותו commit.*
