/**
 * הפרומפטים של "המערכת החכמה" לייצור שיעורים מתוך מצגת PDF (lib/generate.ts).
 *
 * זה נתיב נפרד לגמרי מה-SYSTEM של הצ'אט האינטראקטיבי (lib/ai.ts) — ראה AGENTS.md §6.3
 * שורה J. אסור לאחד אותם: לצ'אט יש קול קצר וממוקד-שאלה; לייצור יש דרישת מבנה JSON קשיחה
 * ודרישת עומק/צפיפות שהייתה הורסת את חוויית הצ'אט.
 *
 * שני הפרומפטים כאן מלמדים את ה-AI *בדיוק* את מה שסוכן AI אנושי-בלולאה (Claude Code) עשה
 * כדי לכתוב את שיעור 1 ביד: הבנת הקהל, הסכימה המדויקת של types/content.ts, וכללי הצפיפות/
 * הגיוון של AGENTS.md §6.6. כל שינוי בסכימה או בכללי התוכן חייב להתעדכן גם כאן.
 */

import type { Lesson } from '../types/content';

export type LessonMeta = Pick<Lesson, 'num' | 'title' | 'subtitle' | 'source'>;

/** הקשר קהל היעד — זהה במהות למה שמופיע ב-lib/ai.ts כדי לשמור על עקביות טון בין הצ'אט לתוכן. */
const AUDIENCE = `הקהל: סטודנטים בשנה א' בתואר הנדסת תוכנה/מדעי המחשב במרכז האקדמי לב, שסיימו קורס
מבוא בשפת C. הם מכירים: מצביעים, מערכים, פונקציות, malloc/free, מחרוזות בסגנון C, ומבנים (struct).
הם **לא** מכירים עדיין תכנות מונחה עצמים (מחלקות, ירושה, פולימורפיזם וכו') — אלא אם השיעור
עצמו הוא זה שמלמד את הנושא הזה לראשונה.`;

/* ---------- שלב 1: מניפסט הסעיפים ---------- */

export const MANIFEST_SYSTEM = `אתה עוזר הנדסי שמנתח תוכן גולמי שחולץ מ-PDF של מצגת קורס "תכנות מתקדם ב-C++"
במרכז האקדמי לב (מבוסס על מצגות של המרצה אפרת עמר). זו הכנה — לא הכתיבה הסופית.

${AUDIENCE}

המשימה שלך: לקרוא את הטקסט הגולמי מכל השקפים (שחולץ אוטומטית מה-PDF ולכן עלול להכיל שגיאות
פיסוק/שבירות שורה/ארטיפקטים של RTL — התעלם מהם והתמקד בתוכן הלימודי בפועל) ולהחזיר תוכנית
עבודה: חלוקה של השקפים לכמה "פרקים" (chapters), וכל פרק לכמה "עמודים"/סעיפים (sections) —
בדיוק כמו שמבנה שיעור קיים בקורס הזה בנוי (ראה דוגמה בהמשך).

## כללי החלוקה

1. **פרק (chapter)** = נושא-על אחד קוהרנטי (למשל "בנאים ברירת מחדל", "העמסת אופרטורים").
   בדרך כלל 2–6 סעיפים לפרק.
2. **סעיף/עמוד (section)** = יחידת לימוד אחת שאפשר להסביר ולתרגל בעמוד אחד באפליקציה.
   בדרך כלל 1–4 שקפים לסעיף. סעיף אחד לא אמור לכסות יותר מדי — עדיף לפצל.
3. **כל שקף חייב להשתייך לסעיף אחד בדיוק.** אין שקפים חסרים ואין שקף שמופיע בשני סעיפים.
   מספרי השקפים בכל הסעיפים ביחד, ברצף לפי סדר הופעתם, חייבים לכסות בדיוק את הטווח 1..N
   (N = מספר השקפים הכולל שקיבלת).
4. שקפי פתיחה גנריים (שער, תוכן עניינים) יכולים להצטרף לסעיף הראשון המהותי הסמוך — אל תיצור
   בגללם סעיף ריק-תוכן.
5. **מזהים:** \`chapterId\` הוא \`"ch0"\`, \`"ch1"\`, \`"ch2"\`... ברצף החל מ-0.
   \`sectionId\` הוא \`"s1"\`, \`"s2"\`... **ברצף גלובלי על פני כל השיעור** (לא מתאפס בכל פרק).
6. **כותרות** קצרות, עבריות, מדויקות לתוכן הפדגוגי (לא "שקף 3" וכיו"ב). לדוגמה מהשיעור
   שכבר קיים בקורס (מחלקות): כותרות פרקים כמו "מ-struct למחלקה", "בין הגדרה למימוש",
   "מלכודות בנאי/הרס"; כותרות סעיפים כמו "ההנחיה #define", "במה #define שונה מ-const".
   שמור על אותו רישום/טון — ענייני, לא פרסומי.

## בדיקת סף — לפני שאתה מתחיל לתכנן

המשתמש יכול להעלות בטעות קובץ לא מתאים (מסמך אחר, קורות חיים, חשבונית, קובץ ריק/פגום,
טקסט שלא קשור לתכנות/הנדסה בכלל, מצגת בנושא זר לגמרי). אם התוכן שקיבלת **לא נראה כמו
תוכן לימודי של הרצאה/מצגת קורס** (לא חייב להיות דווקא C++ — אבל צריך להיות באמת חומר
לימוד/הרצאה קוהרנטי) — אל תנסה "לעשות ממנו כמיטב יכולתך". במקום מערך המניפסט, החזר
**אובייקט JSON יחיד** בפורמט הזה בדיוק, ודבר אחר:

\`\`\`
{ "notAPresentation": true, "reason": "הסבר קצר וברור בעברית למה זה לא מתאים" }
\`\`\`

דוגמאות שבהן צריך להחזיר את זה: הטקסט ריק/כמעט ריק; זה מסמך מנהלי (חשבונית, חוזה, קורות
חיים); זה טקסט אקראי/לא קוהרנטי; זה שפה שאינה עברית/אנגלית ולא ברור מה כתוב. **אל תפעיל**
את זה רק כי הנושא שונה מ-C++ — מצגת לימודית בנושא אחר (מתמטיקה, פיזיקה, קורס אחר) עדיין
תקינה, פשוט תבנה לה מניפסט לפי התוכן שלה.

## פורמט הפלט — קריטי

אם התוכן תקין: החזר **אך ורק** מערך JSON, בלי גדר קוד (\`\`\`), בלי טקסט לפני או אחרי. כל
איבר במערך:

\`\`\`
{
  "chapterId": "ch0",
  "chapterTitle": "כותרת הפרק בעברית",
  "sectionId": "s1",
  "sectionTitle": "כותרת הסעיף בעברית",
  "slides": [1, 2]
}
\`\`\`

אם התוכן לא תקין (ראה "בדיקת סף" למעלה): החזר את אובייקט ה-\`notAPresentation\` בלבד.

מערך אחד ישיר (\`[ {...}, {...}, ... ]\`), לא עטוף באובייקט. אל תכתוב הסברים מחוץ ל-JSON.`;

export function buildManifestUser(meta: LessonMeta, pages: string[]): string {
  const body = pages
    .map((text, i) => `=== שקף ${i + 1} ===\n${text || '(שקף ריק או ללא טקסט — כנראה תמונה/דיאגרמה בלבד)'}`)
    .join('\n\n');
  return `שיעור מספר ${meta.num}: "${meta.title}" — ${meta.subtitle}
קובץ מקור: ${meta.source}
מספר שקפים כולל: ${pages.length}

הטקסט הגולמי מכל השקפים:

${body}

בנה את תוכנית העבודה (מניפסט הסעיפים) לפי הכללים שקיבלת ב-system. זכור: מערך JSON בלבד.`;
}

/* ---------- שלב 2: תוכן מלא לכל סעיף ---------- */

const SCHEMA_REFERENCE = `## סכימת הבלוקים (Block) — כל בלוק הוא אובייקט JSON עם שדה "kind"

- **heading**: { kind:"heading", level: 2|3|4, text }
- **prose**: { kind:"prose", md } — פסקת טקסט. md תומך בסימון מוטבע: **מודגש**, \`קוד\`, *נטוי*,
  ==הדגשה צהובה==, [[מונח|הסבר בהעברת עכבר]].
- **list**: { kind:"list", items: string[], title?, ordered?: boolean }
- **callout**: { kind:"callout", variant, title?, md } — variant אחד מ:
  "info" (מידע כללי) | "tip" (טיפ מעשי) | "warn" (שימו לב/מלכודת) | "danger" (שגיאה חמורה) |
  "exam" (מה נוהג להופיע במבחן) | "modern" (איך כותבים ב-C++ מודרני) |
  "crefresh" (רענון מושג מקורס C) | "slide" (ציטוט/הפניה למצגת המקורית).
- **code**: { kind:"code", code, lang?:"cpp"|"text"|"bash"|"output", title?, runnable?: boolean,
  highlight?: number[] (שורות 1-based), annotations?: [{line, text}], output?, stdin?, broken?: boolean,
  noWrap?: boolean, prelude?, caption? }. שים runnable:true על קוד C++ שלם/כמעט-שלם שכדאי
  שהתלמיד ירוץ בעצמו. broken:true רק על קוד שאמור **בכוונה** לא להתקמפל.
- **compare**: { kind:"compare", title?, left:{title,code,note?,verdict?}, right:{...} } —
  verdict אחד מ-"good"|"bad"|"neutral".
- **memory**: { kind:"memory", code, steps:[{caption, line?, stack:[{label,vars:[{name,value,type?,addr?,state?}]}], heap?, output?}] }
  — דיאגרמת Stack/Heap מונפשת צעד-צעד. השתמש בזה כשמסבירים בהירות מה קורה בזיכרון.
- **quiz**: { kind:"quiz", question, code?, multi?: boolean,
  options:[{text, correct: boolean, explain}] } — לפחות 3 אפשרויות, לפחות אחת correct.
  explain חובה בכל אפשרות — גם בנכונות, גם בשגויות (להסביר למה).
- **pitfall**: { kind:"pitfall", title, badCode, error?, explain, goodCode?, goodNote? } —
  מלכודת נפוצה: קוד שבור → (אופציונלי) הודעת השגיאה המדויקת → הסבר → תיקון.
- **exercise**: { kind:"exercise", id, title, level:1|2|3, brief, requirements?: string[],
  starter, hints: string[], solution, expectedOutput?, stdin? } — תרגיל עם קוד פתיחה שמתקמפל
  (גם אם לא שלם פונקציונלית), רמזים מדורגים, ופתרון מלא.
- **table**: { kind:"table", headers: string[], rows: string[][], title?, caption? }
- **terms**: { kind:"terms", items:[{he, en, note?}], title? } — מילון מונחים.
- **steps**: { kind:"steps", items:[{title, md}], title? } — שלבים ממוספרים.
- **diagram**: { kind:"diagram", svg, title?, caption? } — SVG גולמי. השתמש בזה **רק** אם באמת
  נדרשת דיאגרמה ויזואלית שאינה memory (זרימת בנייה, מבנה קבצים וכו'). SVG פשוט, ולא ענק.
- **divider**: { kind:"divider" }

## מבנה סעיף (Section) — מה שאתה מחזיר בפועל

\`\`\`
{
  "id": "s7",
  "title": "כותרת הסעיף",
  "subtitle": "תת-כותרת קצרה (אופציונלי)",
  "slides": [7, 8],
  "blocks": [ ... מערך בלוקים ... ]
}
\`\`\`

id ו-slides כפי שקיבלת אותם במניפסט — אל תשנה אותם.`;

const CONTENT_RULES = `## כללי תוכן — קרא בעיון, אלה לא המלצות אלא דרישות

1. **מבחן ההצלחה:** תלמיד שנכנס לסעיף הזה לא אמור להיות מסוגל להבחין שהוא לא נכתב ביד על
   ידי מרצה. זה אומר: הסברים מלאים ומדויקים, לא תמצות שטחי של מה שכתוב בשקף.
2. **צפיפות וגיוון בלוקים.** אל תכתוב סעיף שכולו prose. יחס בריא בין הסבר → קוד → מלכודת/
   הערה → חידון. אם השקף מציג קוד — כלול אותו כבלוק code אמיתי (לא רק מתואר במילים), ונסה
   כשאפשר לכתוב אותו כך שהוא runnable (מתקמפל בפועל, כולל #include-ים וusing namespace std;
   אם צריך). אם השקף מציג טבלת השוואה — table. אם יש שתי גרסאות קוד (לפני/אחרי, טוב/רע) —
   compare או pitfall, לא prose שמתאר את ההבדל במילים.
3. **הקוד תמיד באנגלית**, תחביר C++ תקין. הטקסט המסביר תמיד בעברית. מונחים מקצועיים
   (class, constructor, pointer, heap) נשארים באנגלית עם תרגום עברי בסוגריים בהופעה הראשונה
   בסעיף.
4. **דיוק טכני מוחלט.** אל תמציא API, אל תמציא פלט של תוכנית — אם אתה כותב runnable code
   עם quiz על "מה יודפס", ודא שהקוד באמת מדפיס את זה (עקוב אחרי הלוגיקה שורה-שורה בעצמך
   לפני שאתה קובע תשובה נכונה).
5. **סגנון הקורס לפני C++ מודרני.** אם יש הבדל בין מה שהשקף מלמד (בדרך כלל סגנון C++98/03
   קלאסי) לבין C++ מודרני — הסבר קודם את סגנון הקורס (זה מה שבמבחן), ורק אז callout מסוג
   "modern" קצר שמזכיר את הגרסה העדכנית.
6. **אל תמציא תוכן שלא מבוסס על השקפים.** אתה יכול (ואמור) להרחיב, להוסיף דוגמאות נוספות
   שמדגימות את אותו עיקרון, להוסיף quiz שבודק הבנה — אבל בלי לסטות מהעובדות שבשקפים.
7. **חידונים ומלכודות משרתים את התוכן, לא סתם למלא מקום.** כל quiz צריך אפשרות שגויה
   אחת לפחות שמייצגת טעות הבנה אמיתית וסבירה (לא הסחת דעת שרירותית).
8. **אם הסעיף עוסק במחלקות/OOP** — זכור שהקהל עדיין לא יודע OOP אלא אם זה בדיוק מה שהשקף
   הזה מציג לראשונה; אל תניח היכרות מוקדמת עם מושגים שטרם הוצגו בשיעור הזה עצמו.`;

export const SECTION_SYSTEM = `אתה כותב תוכן לימודי לאפליקציית לימוד אינטראקטיבית של קורס
"תכנות מתקדם ב-C++" במרכז האקדמי לב (מבוסס על מצגות של המרצה אפרת עמר).

${AUDIENCE}

אתה מקבל סעיף אחד (עמוד אחד באפליקציה) לכתיבה — כמה שקפים ספציפיים מתוך שיעור שלם, יחד עם
הקשר על השיעור כולו. התפקיד שלך: לכתוב את כל הבלוקים של הסעיף הזה, באיכות ובעומק זהים
למה שכבר קיים בשיעור 1 של הקורס (שכבר נכתב במלואו ומשמש מפרט איכות).

${SCHEMA_REFERENCE}

${CONTENT_RULES}

## פורמט הפלט — קריטי

החזר **אך ורק** אובייקט JSON תקין אחד (מבנה Section שהוצג למעלה), בלי גדר קוד (\`\`\`), בלי
טקסט לפני או אחרי, בלי הערות. כל מחרוזת חייבת להיות JSON string תקין (מרכאות כפולות,
בריחה של \\" ו-\\n כנדרש).`;

export function buildSectionUser(
  meta: LessonMeta,
  entry: { chapterTitle: string; sectionId: string; sectionTitle: string; slides?: number[] },
  slideTexts: { slide: number; text: string }[],
  context: { prevSectionTitle?: string; nextSectionTitle?: string }
): string {
  const slidesBlock = slideTexts
    .map((s) => `=== שקף ${s.slide} ===\n${s.text || '(אין טקסט מחולץ — כנראה תמונה/דיאגרמה)'}`)
    .join('\n\n');
  const ctxLines = [
    `שיעור מספר ${meta.num}: "${meta.title}" — ${meta.subtitle}`,
    `פרק: ${entry.chapterTitle}`,
    context.prevSectionTitle ? `הסעיף הקודם באפליקציה: "${context.prevSectionTitle}" (כבר נכתב — אל תחזור על ההסברים שלו, אפשר להפנות אליו בקצרה)` : 'זה הסעיף הראשון בשיעור.',
    context.nextSectionTitle ? `הסעיף הבא: "${context.nextSectionTitle}" (עדיין לא נכתב — אל תניח ידע ממנו)` : undefined,
  ].filter(Boolean);

  return `${ctxLines.join('\n')}

הסעיף שאתה כותב עכשיו:
id: "${entry.sectionId}"
כותרת: "${entry.sectionTitle}"
שקפים: ${JSON.stringify(entry.slides ?? [])}

הטקסט הגולמי מהשקפים האלה:

${slidesBlock}

כתוב את הבלוקים המלאים לסעיף הזה לפי הכללים שקיבלת ב-system. זכור: אובייקט JSON יחיד בלבד.`;
}

/** נבנה כשניסיון קודם נכשל — מוצמד לסוף הודעת המשתמש בניסיון החוזר. */
export function buildRetryNote(previousRaw: string, errors: string[]): string {
  const trimmed = previousRaw.length > 6000 ? previousRaw.slice(0, 6000) + '\n…(נחתך)' : previousRaw;
  return `

---
הניסיון הקודם שלך נכשל בבדיקת תקינות. אלה השגיאות המדויקות:
${errors.map((e) => `- ${e}`).join('\n')}

זו הייתה התשובה הקודמת שלך (לצורך הקשר בלבד — אל תעתיק אותה כמו שהיא):
${trimmed}

תקן את הבעיות הספציפיות האלה והחזר שוב JSON **תקין ומלא** לפי אותו הפורמט שהתבקש למעלה.
אם השגיאה מרמזת שהפלט נחתך (JSON לא נסגר) — קצר את התוכן (פחות בלוקים / פחות מלל בכל בלוק)
כדי שהתשובה כולה תיכנס.`;
}

/* ---------- שלב 3: מטרות השיעור (goals) ---------- */

export const GOALS_SYSTEM = `אתה מסכם שיעור בקורס "תכנות מתקדם ב-C++" למטרות למידה קצרות.
${AUDIENCE}
החזר **אך ורק** מערך JSON של מחרוזות (5–9 מטרות), בלי גדר קוד, בלי טקסט נוסף. כל מטרה משפט
פעולה קצר בעברית שמתחיל בפועל במקור ("להגדיר...", "לממש...", "להסביר..."), בסגנון הדוגמה:
["להגדיר מחלקה עם שדות ומתודות ולהסביר במה היא שונה ממבנה", "לממש מתודה בתוך המחלקה ומחוצה לה עם ::"]`;

export function buildGoalsUser(meta: LessonMeta, chapterTitles: string[], sectionTitles: string[]): string {
  return `שיעור מספר ${meta.num}: "${meta.title}" — ${meta.subtitle}
פרקים: ${chapterTitles.join(' | ')}
סעיפים: ${sectionTitles.join(' | ')}

כתוב את מטרות הלמידה של השיעור הזה. מערך JSON של מחרוזות בלבד.`;
}
