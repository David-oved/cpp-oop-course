# מפרט חוויית מובייל

מסמך זה קובע כיצד האפליקציה אמורה להתנהג ולהיראות במסכי מובייל — **בנפרד ומעבר** לתיקוני
הבאגים השוטפים (RTL, Dynamic Island, סיידבר). זה עתיד לעבודה, לא בוצע עדיין. כל הרחבה עתידית
של תוכן (שיעורים/מצגות נוספים) צריכה לכבד את המפרט הזה מרגע שהוא ייושם.

## עקרון מנחה

בגרסת מובייל **הכול חייב להיראות שונה לגמרי** ולהתאים למובייל באמת — לא רק "לדחוס" את
פריסת הדסקטופ לרוחב צר. זה חל על: מסך הבית, הסיידבר, מסך ההגדרות, וכל רכיב מרכזי אחר.

## תהליך עבודה נדרש

כדי להגיע לתוצאה מקסימלית, יש לבדוק כל שינוי דרך אמולציית מובייל אמיתית בדפדפן (לא רק לקרוא
קוד CSS) — להיכנס לסביבת הדפדפן, להריץ כאילו משתמש נמצא על מובייל, לראות בפועל איך כל דבר
נראה, ולכוונן לפי מה שנראה בפועל.

## אין עורך קוד (IDE) בגרסת מובייל

- **לא קיים** עורך קוד/סביבת הרצה במובייל בכלל.
- מוצגים **רק** קטעי הקוד כפי שהם מופיעים במצגות הלימוד (כמו היום, קריאה בלבד).
- המשתמש **יכול לראות את הקובץ המלא**, אך **לא להריץ אותו** במובייל.
- המשתמש **יכול לשלוח את הקוד לעוזר ה-AI האישי שלו** (גם אם אינו יכול להריץ אותו).

## תצוגת קובץ קוד מלא

כשמשתמש רוצה לראות קובץ קוד מלא (קבצים יכולים להיות ארוכים) — **לא** "נפתח" ישירות (לא
ניווט לעמוד חדש/לא IDE). במקום זה: נפתח **חלון גלילה במרכז המסך** ובתוכו מוצג קוד הקובץ
המלא, עם ההסברים הרלוונטיים (כמו בשיעור עצמו).

## פאנל ה-AI במובייל — שתי דרכי פתיחה שונות

1. **פתיחה כללית מהצד** — פתיחת הצ'אט כמו היום (פאנל שנפתח מהצד), כשהמשתמש פותח את ה-AI
   באופן כללי (לא מתוך הקשר ספציפי).
2. **פתיחה מתוך שיעור** — כשהמשתמש פונה ל-AI **מתוך תוך כדי שיעור** (שאלה על קטע ספציפי),
   הצ'אט נפתח כ**חלון קופץ במרכז המסך**, לא מהצד — כדי לתת נוחות מרבית תוך כדי קריאה.

## התמדת שיחת ה-AI

השיחה עם ה-AI **לא מתאפסת** בין ניווטים בתוך האפליקציה. היא נשמרת עד שהאפליקציה עצמה
נסגרת (ורק אז מתאפסת) — לא בכל מעבר בין שיעורים/מסכים.

---

*מסמך זה נוצר ב-2026-08-14 לפי הנחיה מפורשת של המשתמש. לפני שמתחילים ליישם — לוודא שכל
הבאגים והפיצ'רים הדחופים שכבר בעבודה (ראו רשימת המשימות הפעילה) הושלמו קודם.*
